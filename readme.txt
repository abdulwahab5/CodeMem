The folder "CodeMem" includes the whole project. (jar file of JAVA code is connected)
	To open Instructor Interface open idex.php
	Enter username "rabiamaqsood@nu.edu.pk" password "123456"

	To open Student Interface open index.html
	Student interface is just the design so you can login using any username and password

The folder "symbolTable" contains the JAVA project.(JAVA code)
	Add external jars to the project symbolTable from the folder "External Libraries(jars)" before running 
	the running the code otherwise it will give errors. 
